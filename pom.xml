<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.bonitasoft.connectors</groupId>
	<artifactId>bonita-connector-servicenow</artifactId>
	<version>1.0.0-beta.1</version>

	<name>Bonita ServiceNow Connector</name>
	<description>ServiceNow Connectors for Bonita</description>
	<url>https://www.bonitasoft.com</url>

	<developers>
		<developer>
			<id>bonitasoft</id>
			<name>Bonitasoft</name>
			<email>rd.user@bonitasoft.com</email>
			<organization>Bonitasoft</organization>
			<organizationUrl>https://www.bonitasoft.com</organizationUrl>
		</developer>
	</developers>

	<licenses>
		<license>
			<name>GPL-v2.0</name>
			<url>http://www.gnu.org/licenses/gpl-2.0.txt</url>
		</license>
	</licenses>

	<properties>
		<!-- Maven -->
		<java.version>17</java.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<maven.build.timestamp.format>yyyy.MM.dd-hh.mm</maven.build.timestamp.format>

		<!-- servicenow-create-incident -->
		<servicenow-create-incident.def.id>servicenow-create-incident</servicenow-create-incident.def.id>
		<servicenow-create-incident.def.version>1.0.0</servicenow-create-incident.def.version>
		<servicenow-create-incident.impl.id>${servicenow-create-incident.def.id}-impl</servicenow-create-incident.impl.id>
		<servicenow-create-incident.impl.version>${project.version}</servicenow-create-incident.impl.version>
		<servicenow-create-incident.main-class>com.bonitasoft.connectors.servicenow.CreateIncidentConnector</servicenow-create-incident.main-class>

		<!-- servicenow-update-incident -->
		<servicenow-update-incident.def.id>servicenow-update-incident</servicenow-update-incident.def.id>
		<servicenow-update-incident.def.version>1.0.0</servicenow-update-incident.def.version>
		<servicenow-update-incident.impl.id>${servicenow-update-incident.def.id}-impl</servicenow-update-incident.impl.id>
		<servicenow-update-incident.impl.version>${project.version}</servicenow-update-incident.impl.version>
		<servicenow-update-incident.main-class>com.bonitasoft.connectors.servicenow.UpdateIncidentConnector</servicenow-update-incident.main-class>

		<!-- servicenow-get-record -->
		<servicenow-get-record.def.id>servicenow-get-record</servicenow-get-record.def.id>
		<servicenow-get-record.def.version>1.0.0</servicenow-get-record.def.version>
		<servicenow-get-record.impl.id>${servicenow-get-record.def.id}-impl</servicenow-get-record.impl.id>
		<servicenow-get-record.impl.version>${project.version}</servicenow-get-record.impl.version>
		<servicenow-get-record.main-class>com.bonitasoft.connectors.servicenow.GetRecordConnector</servicenow-get-record.main-class>

		<!-- servicenow-create-change-request -->
		<servicenow-create-change-request.def.id>servicenow-create-change-request</servicenow-create-change-request.def.id>
		<servicenow-create-change-request.def.version>1.0.0</servicenow-create-change-request.def.version>
		<servicenow-create-change-request.impl.id>${servicenow-create-change-request.def.id}-impl</servicenow-create-change-request.impl.id>
		<servicenow-create-change-request.impl.version>${project.version}</servicenow-create-change-request.impl.version>
		<servicenow-create-change-request.main-class>com.bonitasoft.connectors.servicenow.CreateChangeRequestConnector</servicenow-create-change-request.main-class>

		<!-- servicenow-query-table -->
		<servicenow-query-table.def.id>servicenow-query-table</servicenow-query-table.def.id>
		<servicenow-query-table.def.version>1.0.0</servicenow-query-table.def.version>
		<servicenow-query-table.impl.id>${servicenow-query-table.def.id}-impl</servicenow-query-table.impl.id>
		<servicenow-query-table.impl.version>${project.version}</servicenow-query-table.impl.version>
		<servicenow-query-table.main-class>com.bonitasoft.connectors.servicenow.QueryTableConnector</servicenow-query-table.main-class>

		<!-- servicenow-create-record -->
		<servicenow-create-record.def.id>servicenow-create-record</servicenow-create-record.def.id>
		<servicenow-create-record.def.version>1.0.0</servicenow-create-record.def.version>
		<servicenow-create-record.impl.id>${servicenow-create-record.def.id}-impl</servicenow-create-record.impl.id>
		<servicenow-create-record.impl.version>${project.version}</servicenow-create-record.impl.version>
		<servicenow-create-record.main-class>com.bonitasoft.connectors.servicenow.CreateRecordConnector</servicenow-create-record.main-class>

		<!-- servicenow-update-record -->
		<servicenow-update-record.def.id>servicenow-update-record</servicenow-update-record.def.id>
		<servicenow-update-record.def.version>1.0.0</servicenow-update-record.def.version>
		<servicenow-update-record.impl.id>${servicenow-update-record.def.id}-impl</servicenow-update-record.impl.id>
		<servicenow-update-record.impl.version>${project.version}</servicenow-update-record.impl.version>
		<servicenow-update-record.main-class>com.bonitasoft.connectors.servicenow.UpdateRecordConnector</servicenow-update-record.main-class>

		<!-- servicenow-add-journal -->
		<servicenow-add-journal.def.id>servicenow-add-journal</servicenow-add-journal.def.id>
		<servicenow-add-journal.def.version>1.0.0</servicenow-add-journal.def.version>
		<servicenow-add-journal.impl.id>${servicenow-add-journal.def.id}-impl</servicenow-add-journal.impl.id>
		<servicenow-add-journal.impl.version>${project.version}</servicenow-add-journal.impl.version>
		<servicenow-add-journal.main-class>com.bonitasoft.connectors.servicenow.AddJournalConnector</servicenow-add-journal.main-class>

		<!-- Bonita -->
		<bonita-common.version>10.2.0</bonita-common.version>

		<!-- Connector dependencies -->
		<lombok.version>1.18.30</lombok.version>
		<jackson.version>2.17.0</jackson.version>

		<!-- Test -->
		<junit-jupiter.version>5.10.2</junit-jupiter.version>
		<mockito.version>5.11.0</mockito.version>
		<assertj.version>3.27.7</assertj.version>
		<jqwik.version>1.8.4</jqwik.version>
		<wiremock.version>3.4.2</wiremock.version>
		<testcontainers.version>1.19.6</testcontainers.version>
		<bonita-java-client.version>1.0.1</bonita-java-client.version>
		<awaitility.version>4.2.0</awaitility.version>

		<!-- Maven plugins -->
		<maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
		<maven.compiler.release>${java.version}</maven.compiler.release>
		<maven-assembly-plugin.version>3.7.1</maven-assembly-plugin.version>
		<groovy-maven-plugin.version>2.1.1</groovy-maven-plugin.version>
		<groovy.version>3.0.19</groovy.version>
		<maven-surefire-plugin.version>3.2.5</maven-surefire-plugin.version>
		<maven-failsafe-plugin.version>3.2.5</maven-failsafe-plugin.version>
		<jacoco-maven-plugin.version>0.8.11</jacoco-maven-plugin.version>
		<maven-source-plugin.version>3.3.0</maven-source-plugin.version>
		<maven-javadoc-plugin.version>3.7.0</maven-javadoc-plugin.version>
		<central-maven-plugin.version>0.8.0</central-maven-plugin.version>
	</properties>

	<dependencies>
		<!-- Bonita -->
		<dependency>
			<groupId>org.bonitasoft.engine</groupId>
			<artifactId>bonita-common</artifactId>
			<version>${bonita-common.version}</version>
			<scope>provided</scope>
		</dependency>

		<!-- Connector -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<version>${lombok.version}</version>
			<scope>provided</scope>
		</dependency>

		<!-- Jackson for JSON -->
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
			<version>${jackson.version}</version>
			<scope>provided</scope>
		</dependency>

		<!-- Tests -->
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>${junit-jupiter.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-core</artifactId>
			<version>${mockito.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-junit-jupiter</artifactId>
			<version>${mockito.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
			<version>${assertj.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>net.jqwik</groupId>
			<artifactId>jqwik</artifactId>
			<version>${jqwik.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.wiremock</groupId>
			<artifactId>wiremock-standalone</artifactId>
			<version>${wiremock.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers</artifactId>
			<version>${testcontainers.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>${testcontainers.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.bonitasoft.web</groupId>
			<artifactId>bonita-java-client</artifactId>
			<version>${bonita-java-client.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.awaitility</groupId>
			<artifactId>awaitility</artifactId>
			<version>${awaitility.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
			<version>1.4.14</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<defaultGoal>verify</defaultGoal>
		<resources>
			<resource>
				<directory>src/main/resources-filtered</directory>
				<filtering>true</filtering>
			</resource>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>false</filtering>
			</resource>
		</resources>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>${maven-compiler-plugin.version}</version>
					<configuration>
						<annotationProcessorPaths>
							<path>
								<groupId>org.projectlombok</groupId>
								<artifactId>lombok</artifactId>
								<version>${lombok.version}</version>
							</path>
						</annotationProcessorPaths>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-surefire-plugin</artifactId>
					<version>${maven-surefire-plugin.version}</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-failsafe-plugin</artifactId>
					<version>${maven-failsafe-plugin.version}</version>
				</plugin>
				<plugin>
					<artifactId>maven-assembly-plugin</artifactId>
					<version>${maven-assembly-plugin.version}</version>
					<configuration>
						<descriptorSourceDirectory>
							${project.basedir}/src/assembly</descriptorSourceDirectory>
					</configuration>
					<executions>
						<execution>
							<id>package-connectors</id>
							<phase>package</phase>
							<goals>
								<goal>single</goal>
							</goals>
							<configuration>
								<appendAssemblyId>true</appendAssemblyId>
							</configuration>
						</execution>
					</executions>
				</plugin>
				<plugin>
					<groupId>org.codehaus.gmaven</groupId>
					<artifactId>groovy-maven-plugin</artifactId>
					<version>${groovy-maven-plugin.version}</version>
					<dependencies>
						<dependency>
							<groupId>org.codehaus.groovy</groupId>
							<artifactId>groovy</artifactId>
							<version>${groovy.version}</version>
						</dependency>
					</dependencies>
				</plugin>
				<plugin>
					<groupId>org.jacoco</groupId>
					<artifactId>jacoco-maven-plugin</artifactId>
					<version>${jacoco-maven-plugin.version}</version>
					<configuration>
						<append>true</append>
					</configuration>
					<executions>
						<execution>
							<id>prepare-agent</id>
							<goals>
								<goal>prepare-agent</goal>
							</goals>
						</execution>
						<execution>
							<id>prepare-agent-integration</id>
							<goals>
								<goal>prepare-agent-integration</goal>
							</goals>
						</execution>
						<execution>
							<id>jacoco-site</id>
							<phase>verify</phase>
							<goals>
								<goal>report</goal>
							</goals>
						</execution>
					</executions>
				</plugin>
				<plugin>
					<groupId>org.sonatype.central</groupId>
					<artifactId>central-publishing-maven-plugin</artifactId>
					<version>${central-maven-plugin.version}</version>
					<extensions>true</extensions>
					<configuration>
						<autoPublish>true</autoPublish>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>

		<plugins>
			<plugin>
				<groupId>org.codehaus.gmaven</groupId>
				<artifactId>groovy-maven-plugin</artifactId>
				<executions>
					<execution>
						<phase>generate-resources</phase>
						<goals>
							<goal>execute</goal>
						</goals>
						<configuration>
							<source>
								${project.basedir}/src/script/dependencies-as-var.groovy</source>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>${maven-source-plugin.version}</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<goals>
							<goal>jar-no-fork</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>${maven-javadoc-plugin.version}</version>
				<configuration>
					<doclint>none</doclint>
					<failOnError>false</failOnError>
				</configuration>
				<executions>
					<execution>
						<id>attach-javadocs</id>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.sonatype.central</groupId>
				<artifactId>central-publishing-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>deploy</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<version>3.2.8</version>
						<executions>
							<execution>
								<id>sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
								<configuration>
									<gpgArguments>
										<arg>--pinentry-mode</arg>
										<arg>loopback</arg>
									</gpgArguments>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

	<repositories>
		<repository>
			<id>bonitasoft-releases</id>
			<url>https://bonitasoft.jfrog.io/artifactory/maven</url>
		</repository>
	</repositories>

	<scm>
		<developerConnection>scm:git:git@github.com:bonitasoft/bonita-connector-servicenow.git</developerConnection>
		<connection>scm:git:git@github.com:bonitasoft/bonita-connector-servicenow.git</connection>
		<url>https://github.com/bonitasoft/bonita-connector-servicenow</url>
		<tag>HEAD</tag>
	</scm>

</project>
